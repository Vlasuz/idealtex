import styled from "styled-components";

export const ProductsNotFoundStyle = styled.div`
    text-align: center;

    h2{
        font-size: 32px;
        font-weight: 700;
        letter-spacing: 6px;
        color: var(--darkblue);
        margin-bottom: 10px;
    }
`